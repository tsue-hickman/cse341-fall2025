describe('Movies API', () => {
  test('Basic math works', () => {
    expect(1 + 1).toBe(2);
  });
  
  test('Array operations work', () => {
    const movies = [];
    movies.push({ title: 'Test Movie' });
    expect(movies.length).toBe(1);
  });
});